# Use a base image with a pre-installed web server
FROM nginx:latest 
# Copy the web application files to the default web server directory 
COPY . /usr/share/nginx/html 
# Expose port 80 to allow incoming traffic
EXPOSE 80
